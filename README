# itest

This is a small tools to test v2x api bridge on the seco_libs layer

## Build

$ make clean

$ make CC=<PATH_TO_TOOLCHAIN> ARCH=arm64 HSM_PATH=<PATH_SECO_LIBS>

## Help

Param:

  -l list all tests

  -t <test_name> run the test in param
