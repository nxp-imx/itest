# V2XA lib test

This is a small tools to test v2x api bridge on the seco_libs layer

## Build

make all

make clean

make PREFIX=<TOOLCHAIN_PREFIX_PATH> ARCH=aarch64 HSM_PATH=<PATH_SECO_LIBS>

## Help

Param:

  -l list all tests

  -t <test_name> run the test in param
